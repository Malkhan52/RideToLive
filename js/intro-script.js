$(
  (function(window, document, $, undefined) {
    "use strict";

    // Set up HPT object
    window.HPT = {};

    App.init();
    Debug.init();
    Onboarding.init();

    var syncId;
  })(window, document, jQuery)
);